namespace Soundbox
{
    /// <summary>
    /// Used to organize which set of results uses which block of integer codes.
    /// No functionality at runtime
    /// </summary>
    public enum ResultStatusSegment
    {
        File = 1000,
        Volume = 2000,
        Speech = 3000
    }
}
