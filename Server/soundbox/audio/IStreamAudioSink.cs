namespace Soundbox.Audio
{
    /// <summary>
    /// Audio sink that accepts audio as a stream of bytes in some specified format (PCM with parameters for sample rate, bit depth etc; MP3;...)<br/>
    /// Doesn't actually do anything yet and is here only as a placeholder.
    /// </summary>
    public interface IStreamAudioSink : IAudioSink
    {
    }
}
