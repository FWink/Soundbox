namespace Soundbox.Audio
{
    /// <summary>
    /// Audio sink that accepts uncompressed audio as a stream of bytes in some specified format (PCM with parameters for sample rate, bit depth etc)<br/>
    /// Doesn't actually do anything yet and is here only as a placeholder.
    /// </summary>
    public interface IStreamAudioSink : IAudioSink
    {
    }
}
